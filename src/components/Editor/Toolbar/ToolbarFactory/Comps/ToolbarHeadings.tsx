import React from "react";
import { ToolbarComponentProps } from "../ToolbarCompFactory";


export const ToolbarHeadings: React.FC<ToolbarComponentProps> = ({ editor }) => {
    return (
        <div>test</div>
    )
}